type Query {
  orders: [Order]
}

type Order {
  id: ID!
  date: String!
  subtotal: Float
  items: [OrderItem]
}

type Review {
  rating: Int!
  description: String!
}

type OrderItem {
  product: Product!
  quantity: Int!
}
